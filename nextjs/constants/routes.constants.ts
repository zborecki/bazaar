export enum GLOBAL_ROUTE {
  ROOT = '/',
  UNKNOWN = '*'
}

/**
 * Internal routes.
 *
 * If you make changes to the values in this enumeration, please remember to update
 * the corresponding entries in the `pathnames.i18n.ts`.
 */
export enum ROUTE {
  WISHLIST = '/wishlist'
}
